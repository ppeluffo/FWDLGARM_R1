# Build de CubeIDE
/Debug/
/Release/

# Workspace de Eclipse (si el workspace apunta acá)
/.metadata/
*.launch

# Preferencias locales del editor (formato de doxygen y demás). No afectan al
# build: eso vive en .cproject, que SÍ se versiona.
/.settings/

# Backups de CubeMX
*.ioc.bak
